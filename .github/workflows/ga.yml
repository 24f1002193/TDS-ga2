name: TDS GA2

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: 24f1002193@ds.study.iitm.ac.in
        run: echo "Hello, world!"
